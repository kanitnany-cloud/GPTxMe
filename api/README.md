# ESP32 S3 Display API

Free static endpoints for the AI x Kanitnan FPL Quest display.

- `esp32-config.json`: endpoint map and screen refresh plan
- `fpl-device-snapshot.json`: current dashboard snapshot for the 3.5 inch ESP32 S3 screen

Live sources are public URLs listed inside `esp32-config.json`.
